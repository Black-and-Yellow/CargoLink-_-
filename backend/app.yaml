runtime: nodejs20
env: standard
instance_class: F1

handlers:
  - url: /.*
    script: auto
    secure: always

env_variables:
  JWT_ACCESS_EXPIRY: "30m"
  JWT_REFRESH_EXPIRY: "7d"
  # Remaining secrets are appended here at build time by Cloud Build from Secret Manager